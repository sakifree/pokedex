<!DOCTYPE html>
<html lang="en">
    <%- include ("../partials/head.ejs") %>
<body>
    <%- include ("../partials/header.ejs") %>
    <main>
        <div>
            <% for (poke of pokemon) { %>
                <a href="/pokemon/<%= poke.id %>"><img src="<%= poke.img %>" alt="<%= poke.name %>"></a>
            <% }%>    
        </div>
    </main>
</body>
</html>