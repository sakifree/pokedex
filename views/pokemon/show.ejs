<!DOCTYPE html>
<html lang="en">
    <%- include ("../partials/head.ejs") %>
<body>
    <%- include ("../partials/header.ejs") %>
    <h1><%= poke.name  %></h1>
    <h1><img src="<%= poke.img  %>" alt="<%= poke.name  %>"></h1>
    <div id="stats-type">
    <h3>Type</h3> <% for (let i=0; i < poke.type.length; i++) { %>
        <ul>
            <li><%= poke.type[i] %></li>
        </ul>
    <% } %>    
    <h3>Stats</h3>
        HP: <%= poke.stats.hp %> <br>
        Attack: <%= poke.stats.attack %> <br>
        Defense: <%= poke.stats.defense %> <br>
    </div>
    <div id="edit-delete">
    <form action="/pokemon/<%= index %>?_method=DELETE" method="POST">
        <input type="submit" value="DELETE">
    </form>
    <a href="/pokemon/<%= index %>/edit"><button>EDIT</button></a> 
    </div>
</body>
</html>